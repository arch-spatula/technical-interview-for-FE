/**
 * @param {number} num1
 * @param {number} num2
 * @returns {number}
 */
function LCM(num1, num2) {
  let multiple = 2;
  // while (num1) {}
  return 1;
}

function solution(n) {
  return n;
}

export default solution;
